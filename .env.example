# ==========================================
# CookWire 環境設定ファイル（.envにコピーして使用）
# ==========================================
# 重要: .envファイルは絶対にGitにコミットしないこと！

# ------------------------------------------
# フロントエンド設定（Vite）
# ------------------------------------------
VITE_API_BASE_URL=http://localhost:3001/api

# ------------------------------------------
# バックエンド設定（Express Server）
# ------------------------------------------
# サーバーポート（デフォルト: 3001）
PORT=3001

# データ保存ディレクトリ（デフォルト: ./data）
# 注意: このディレクトリは適切なパーミッションで保護すること
DATA_DIR=./data

# データベースファイルパス（デフォルト: ./data/projects.db）
DATABASE_PATH=./data/projects.db

# CORS許可オリジン（カンマ区切り、デフォルト: http://localhost:5173）
# 本番環境では具体的なドメインを指定すること
# 例: CORS_ALLOW_ORIGINS=https://app.example.com
CORS_ALLOW_ORIGINS=http://localhost:5173

# 環境モード（development/production、デフォルト: development）
NODE_ENV=development

# Trust Proxy設定（プロキシ背後で動作する場合は 'true'）
# 注意: 'true' にするとX-Forwarded-Forヘッダーを信頼するようになる
TRUST_PROXY=false
